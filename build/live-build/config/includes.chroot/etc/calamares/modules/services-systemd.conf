---
# services-systemd.conf — services to enable/disable on the installed system.
#
# These run in the installed system's chroot after unpackfs.

enable:
  - docker.service
  - zerotier-one.service
  - meshnode-wizard.service   # wizard runs once; disables itself after completion
  - ssh.service

disable:
  []
